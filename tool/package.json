{
  "name": "card-year-builder",
  "version": "1.0.1",
  "description": "Build the necessary files for a year",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf outDir",
    "compile": "tsc --build tsconfig.json --pretty",
    "build-final": "cpy package.json outDir && cd outDir/ && npm install --production",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npx nodemon"
  },
  "author": "Geoff Soord",
  "license": "Apache License",
  "dependencies": {
    "@types/node": "^20.11.16",
    "axios": "^1.6.3",
    "fetch": "^1.1.0",
    "fs": "^0.0.1-security",
    "i18next": "^23.7.15",
    "nodemon": "^3.0.3",
    "sharp": "^0.33.2",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/i18next-sprintf-postprocessor": "^0.2.3",
    "cpy-cli": "^5.0.0",
    "nodemon": "^3.0.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
